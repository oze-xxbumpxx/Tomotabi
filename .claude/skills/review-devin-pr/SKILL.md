---
name: review-devin-pr
description: >
  Devin など他のエージェントが Issue から作った PR を、Claude Code がレビューする手順。
  `gh issue create` の後に wait-for-pr.mjs で PR を待ち、見つかって呼び戻されたときに使う。
  「PR ができた」「Devin の PR をレビューして」と言われたときにも使う。コードは直さず、
  指摘はユーザーの確認を取ってから PR にコメントする。
---

# 他エージェントの PR のレビュー

## 待機（Issue を渡した直後）

1. `gh issue create` の後、フック（suggest-pr-watch）が促したら、Bash の `run_in_background` で
   `node .claude/scripts/wait-for-pr.mjs <Issue 番号>` を起動する。自分で実装する Issue では起動しない。
2. 複数の Issue を渡したときは、Issue ごとに 1 本ずつ起動する。
3. 待機中は `sleep` や `gh` の繰り返しで様子を見ない。終了すると呼び戻される。
   - 終了コード 0: 最後の行の JSON に PR 番号がある → 下の「レビュー」へ
   - 終了コード 3: 時間切れ → ユーザーに伝え、再度待つかを聞く

## レビュー

1. **状態**: `gh pr view <n> --json files,statusCheckRollup,mergeable,body`。CI が未完了なら、
   ci-monitor の通知か、`gh pr checks <n> --watch` を `run_in_background` で待つ（ポーリングしない）。
2. **範囲**: 変更ファイルが Issue の「やること」と範囲内か。範囲外のファイル、並行作業中の他 PR が作る
   はずの型・ファイルを先回りで作っていないか。本文に `Closes #<Issue>` があるか。
3. **中身**: 設計書・試験計画と突き合わせる。名前・置き場所・型・エラーコード・応答の形。
   Issue に書いた申し送り（前の PR からの持ち越し）が守られているか。
4. **テスト**: 試験計画の観点 ID がすべてあるか。テストが本当にその経路を通っているか
   （例: #35 は素の HTTP サーバーで試し、Nest の例外経路を通っていなかった）。
5. **セキュリティ**（認証・ログ・外部入力を触る PR）: 迂回できないか、秘密がログや応答に出ないか、
   テスト用の仕組みが本番に混ざらないか。疑わしければ一時的な worktree で**実際に動かして再現**する。
   再現用のテストはコミットしない。worktree は `git worktree remove` で片付ける。
6. **衝突**: 並行 PR が同じファイル（`logs/` など）を触っていないか。マージ順を提案する。

## 報告

- PR ごとに「マージ可 / 手直し / 修正が必要」を先に書き、根拠・再現結果・後続への申し送りを続ける。
- PR へのコメントは**文面をユーザーに見せて了承を得てから** `gh pr comment` で投稿する。
- マージはユーザーが行う。Claude Code はマージしない。
- 衝突の解消を頼まれたら、worktree で main を取り込み、両方の記述を残して解消し、lint・型・テストを通してから push する。
  修正中のエージェントがいるブランチには、同時にコミットしない。
