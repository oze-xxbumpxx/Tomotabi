## 対応概要

<!-- 何を・なぜ を 1〜3 行で。「〜のため、〜を変えた」の形。L1 は 3 行以内の要約でよい。 -->

- 変更レベル: L0 / L1 / L2 / L3
- ユーザー承認: 必要 / 不要

## 変更内容

<!-- 何をどうしたかを箇条書き。「<対象>: <追加 / 変更 / 削除したもの>」の形で、diff を読まなくても把握できる粒度にする。 -->

-

## 実装詳細・設計判断

<!-- なぜそう設計したか・別の方法との比較（L2 / L3 は必須。L0 / L1 は省略可）。 -->

## 品質確認

<!-- 実行したコマンドと結果。未実行の項目は理由を添えて「未確認」と書く。 -->

- [ ] `npm run lint`
- [ ] `npm run type-check`
- [ ] `npm test`
- [ ] `npm run build`

<!-- 該当する場合に追加: API 契約の変更 → `npm run api:check`、DB スキーマの変更 → `npm run db:check`、.claude/・.agents/・AGENTS.md の変更 → `npm run test:harness` -->
