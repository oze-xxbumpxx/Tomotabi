# 2026-09-10

## 今日のタスク

現状のハーネス構成を分析し、惜しい点・改善ポイントを HTML レポートにまとめる。
変更レベル: Level 0（調査・分析。コード・構成ファイルは変更しない）。
ブランチ: `cursor/harness-review-report-5eb0`。Sprint: 未設定。

## やったこと

- [x] hooks 7 / lib 2 / scripts 11 / skills 15 / agents 10 / docs/claude-code / evals / logs を通読。
- [x] 文書・スクリプト内のパス参照を抽出して実在確認（実運用に影響する参照切れ 29 件）。
- [x] Cookpit 固有語（pnpm / Vitest / Drizzle / Hono / apps/web / contract-designer 等）の残存数を集計。
- [x] `node --test .claude/tests/*.test.mjs` を実行し 88 件成功を確認。各フックの起動時間を計測（19〜33 ms）。
- [x] 分析結果を `docs/reports/harness-review-2026-09-10.html` に作成（総評 / 良い点 / 改善 17 件（高 4・中 7・低 6）/ 参照切れ一覧 / 進め方 / 学びのポイント / 付録）。

## 気づき・メモ

- 基盤（harness-paths / harness-state / guard-dangerous）は堅牢で変更不要。問題はワークフロー層・改善層に残る Cookpit 前提。
- `check-deliverables.mjs` の `isSourceFile()` は `packages/*/src` と `apps/web/src` 固定のため、Tomotabi では発火しない。
- `run-quality-gates.sh` の harness ゲートは `test:harness` script 前提で、`node --test` にフォールバックしない（AGENTS.md の指示と食い違う）。
- `orchestrator.md` の tools と `agent-responsibilities.md` に `contract-designer` があるが定義ファイルは無い（「11 本」の記載に対し実体 10 本）。
- `validate-agent-config.mjs` の model 検証は公式エイリアス（sonnet / opus / haiku / inherit）をブロックする。
- `current-feature` はスキルが旧パス（`.claude/state/`）へ書けと指示し、フックは永続領域を優先して読むため、古い値が新しい値を隠しうる。
- 配布元 cookpit は非公開のため plugin 側フック定義は未確認。project settings との二重発火は要確認事項として記載。

## AI ツール活用記録

- Cursor Cloud Agent（Claude Fable 5.1）: ハーネス全体の通読・機械チェック・HTML レポート作成。サブエージェントは使用せず単一モデルで実施。

## 次回やること

- [ ] レポート第 6 章の順序で棚卸しを実施する（`audit-skills` スキル）。まず H-2（参照切れ）と H-3（contract-designer / 11 本 / Cookpit 自己紹介）。
- [ ] AGENTS.md に「現在有効なハーネス機能 / 休眠」節を追加する（H-4）。
- [ ] `run-quality-gates.sh` に `node --test` フォールバックを入れる（H-1）。
- [ ] 新セッションで record-activity の行数を見て plugin フックの二重発火を確認する（M-4）。
- [ ] （前回からの持ち越し）ユーザーのアプリ要件を確認し、MVP と技術スタックを決める。

## 所要時間

約 15 分（活動ログ未導入のため会話上の経過から推定・単一セッション）

---

## セッション 2（同日追記）: 追加分析

依頼: 「他に分析して改善できる部分がないかを見てみてください」。
第 1 回は静的な通読が中心だったため、第 2 回はフックやスクリプトを **実際に動かして** 振る舞いを確かめた。
変更レベル: Level 0（レポートとログのみ追加。ハーネスのコード・設定は変更していない）。

### やったこと

- [x] `guard-dangerous.mjs` に破壊的コマンド 25 種を stdin 経由で投入し、ALLOW / DENY を実測（JSON は Node の `JSON.stringify` で生成）。
- [x] `validate-agent-config.mjs` に補助 `.md` を持つスキル・`model: inherit` を持つエージェントを与え、誤ブロックを確認。
- [x] `.claude/tests/*.test.mjs` をファイル単位で計測し、所要時間の偏りを確認（全体 30.4 s、review-readiness 系が大半）。
- [x] Claude Code 公式 hooks 仕様（2026-09-10 時点）と現行 `settings.json` の差分を確認。
- [x] レポートに「8. 追加分析（第 2 回）」（22 件）と付録 9.5「第 2 回で使った実測方法」を追記。総評に追記コールアウトを追加。

### 見つかったこと（要点）

- **E-1（中）** 開発方針（**AI 駆動開発を主とする**。AI が実装し、ユーザーは要件・設計判断・レビュー・マージを担う）と「人が止まって判断するゲート」が AGENTS.md / CLAUDE.md に書かれていない。ハーネスの既定（orchestrator → implementer）は方針と一致しているので、書くべきは「任せる範囲」より「人が判断する点」。
  - 訂正: 初版では「AI は提案のみ、コードはユーザーが書く」という古い前提で「高」としていたが、ユーザー確認により AI 駆動開発が方針と判明したため、E-1 / E-3 を書き直した。
- **E-3（中）** AI が書いたコードを「読んで学ぶ」ための説明（なぜこの構成か・別解・現場の慣行）を出す役割が無い。`explain-change` スキルか implementer 完了報告の「設計判断」節として、L2/L3 で必須・L1 は要約・L0 は省略と変更レベルに紐づける案。
- **A-1〜A-3（高）** `git clean -fd` / `git checkout .` / `git stash drop` / `git branch -D` / `git push --delete`、DB drop（Sequelize / Prisma / psql）、`aws s3 rb` / `ec2 terminate-instances`、`find -delete`、`rm -rf ./` が guard を素通り（25 種中 12 種 ALLOW）。`secret/` ディレクトリ名で誤検知もある。`permissions.deny` で `.env` 等を守る案を併記。
- **C-1（高）** スキル配下の補助 `.md`（例: `.claude/skills/x/reference.md`）が `validate-agent-config.mjs` で SKILL.md として検証されブロックされる。**C-2** `model: inherit` も同様。
- **B-1（中）** `record-subagent.mjs` が `agent_type` を保存していないため、どの Agent が動いたか後から追えない。**B-2 / B-3** `ConfigChange` / `SessionEnd` / `PreCompact` イベントが未活用。Stop の `additionalContext` と `.claude/rules/` の自動読込は公式に対応済み（良い知らせ）。
- **D-1（中）** `estimate-session-time.mjs` が `git log --since="48 hours ago"` 固定で、前日の作業を今日の所要時間に混ぜうる。**D-2** TZ 指定が UTC / `HARNESS_TZ` / 無指定の三通り混在。
- **F-1（中）** Codex 側にはフックの保護が一切なく、代替の運用ルールも文書化されていない。

### 次回やること（追加）

- [x] **最優先**: AGENTS.md に「作業分担（AI 駆動開発）」節を追加する。→ セッション 3
- [ ] `guard-dangerous.mjs` に git 作業ツリー破壊系・DB drop・AWS 削除系のパターンを追加し、テストも追加する（A-1〜A-3）。
- [ ] `validate-agent-config.mjs` の `isSkill` を `SKILL.md` 限定にし、model 検証に公式エイリアスを許可する（C-1 / C-2 / 第 1 回 M-2）。
- [ ] `record-subagent.mjs` に `agent_type` を保存する（B-1）。

### 所要時間（セッション 2）

約 20 分（会話上の経過から推定）

---

## セッション 3（同日追記）: 作業分担（AI 駆動開発）

依頼: 「お願いします」。モデルは Grok 4.6。
変更レベル: Level 1。ユーザー承認: 不要（入口文書と、分類・委譲・完了報告の接続）。
ブランチ: `cursor/agents-work-allocation-5eb0`。

### やったこと

- [x] `AGENTS.md` に作業分担節（AI 駆動開発・人が判断するゲート表）を追加。作業の進め方にブランチ / PR を 1 項追加。Codex にフックが無いことの代替ルールを入口へ追加。
- [x] `.claude/skills/classify-change/SKILL.md` の出力に「ユーザー承認: 必要 / 不要」を追加。
- [x] `.claude/agents/orchestrator.md` の判定提示と委譲フロー早見に、L2/L3 の設計承認ゲートを追加。Cookpit 自己紹介や contract-designer 参照は触っていない。
- [x] `.claude/agents/implementer.md` の完了報告に、変更レベル別の設計判断説明を追加。
- [x] PR #3 を作成。`node --test .claude/tests/*.test.mjs` は 88 件成功。lint / type-check はアプリ未実装のため unknown。

### 気づき・メモ

- ハーネスの既定（orchestrator → implementer）は AI 駆動開発と一致している。今回書いたのは「任せる範囲」ではなく「人が止まって判断する点」。
- レビューレポート（PR #2）が `main` に入ったあと、同日ログはセッション 1 / 2 と結合した。

### AI ツール活用記録

- Cursor Cloud Agent（Grok 4.6）: ユーザー指示によりモデルは固定。サブエージェントは使っていない。

### 次回やること

- [x] レポート第 6 章の順序で棚卸し（`audit-skills`）。まず H-2（参照切れ）と H-3（contract-designer / 11 本 / Cookpit 自己紹介）。→ セッション 4
- [x] AGENTS.md に「現在有効なハーネス機能 / 休眠」節を追加する（H-4）。→ セッション 4
- [ ] `guard-dangerous.mjs` に git 作業ツリー破壊系・DB drop・AWS 削除系のパターンを追加する（A-1〜A-3）。
- [ ] （持ち越し）ユーザーのアプリ要件を確認し、MVP と技術スタックを決める。

### 所要時間（セッション 3）

記録なし

---

## セッション 4（同日追記）: 棚卸し H-2 / H-3 / H-4

依頼: 「同じモデルで進めましょう」。モデルは Grok 4.6 のまま。
変更レベル: Level 1。ユーザー承認: 不要（死んだ参照と事実誤りの修正。継続依頼済み）。
ブランチ: `cursor/harness-audit-h2-h3-5eb0`（`cursor/agents-work-allocation-5eb0` から分岐）。

### やったこと

- [x] H-3: `contract-designer` を tools / 一覧 / 委譲フローから除去。常備 Agent を 11→10。orchestrator / architecture-designer の Cookpit・DDD 前提を `AGENTS.md` 参照に置換。security-reviewer の観点を FW 非依存に戻した。
- [x] H-2: `docs/01`〜`07`・層別ルール・harness-state.md・archive・欠落 evals 4 件・欠落 ADR・Cursor 専用パスへの「必ず読め」を、削除 / `AGENTS.md` 置換 / 「未作成」注記の 3 択で処理。
- [x] 空の `docs/claude-code/improvements/improvement-backlog.md` を作成。承認境界の正典を `improvement-cycle.md` に集約。
- [x] H-4: `AGENTS.md` に「現在有効なハーネス機能 / 休眠」を追加。
- [x] ADR 採番を Tomotabi は ADR-0001 から開始に修正。evals README を実在 6 件に合わせた。
- [x] PR #4 を作成。`node --test .claude/tests/*.test.mjs` は 88 件成功。

### 次回やること

- [x] `guard-dangerous.mjs` に git 作業ツリー破壊系・DB drop・AWS 削除系のパターンを追加する（A-1〜A-3）。→ セッション 5
- [ ] `validate-agent-config.mjs` の `isSkill` を `SKILL.md` 限定にし、model 検証に公式エイリアスを許可する（C-1 / C-2）。
- [ ] （持ち越し）ユーザーのアプリ要件を確認し、MVP と技術スタックを決める。

### 所要時間（セッション 4）

記録なし

---

## セッション 5（同日追記）: guard-dangerous A-1〜A-5

依頼: 「次も同じモデルで行っていきましょう」。モデルは Grok 4.6 のまま。
変更レベル: Level 1。ユーザー承認: 不要（既存ガードへの deny 追加。継続依頼済み）。
ブランチ: `cursor/guard-dangerous-a1-a3-5eb0`（棚卸しブランチから分岐）。

### やったこと

- [x] A-1: `git clean -fd` / `checkout|restore .` / `stash drop|clear` / `branch -D` / `push --delete` を deny。dry-run（`-n`）と単一ファイル restore は許可。
- [x] A-2: sequelize db:drop / prisma migrate reset / DROP DATABASE / dropdb は本番語（prod / production）があるときだけ deny。ローカル drop は許可。
- [x] A-3: AWS を `s3 rm|rb` / `delete-*` / `terminate-*` / `--profile prod` に拡大。`rm -rf ./` `../` `/var` 等を壊滅的ターゲットへ追加。
- [x] A-4: `settings.json` に `permissions.deny`（`.env` / `.pem` / `id_rsa`）。`.env.example` は含めない。
- [x] A-5: `docs/secret/*.md` は許可し、`secrets/*.json` や拡張子なしは拒否。
- [x] テスト 3 本を追加。`node --test .claude/tests/*.test.mjs` は 91 件成功（新規 3）。PR #5。

### 次回やること

- [x] `validate-agent-config.mjs` の `isSkill` を `SKILL.md` 限定にし、model 検証に公式エイリアスを許可する（C-1 / C-2）。→ セッション 6
- [ ] （持ち越し）ユーザーのアプリ要件を確認し、MVP と技術スタックを決める。

### 所要時間（セッション 5）

記録なし

---

## セッション 6（同日追記）: validate-agent-config C-1 / C-2 / C-3

依頼: 「同じモデルで続けましょう」。モデルは Grok 4.6 のまま。
変更レベル: Level 1。ユーザー承認: 不要（誤ブロックの修正。継続依頼済み）。
ブランチ: `cursor/validate-agent-config-c1-c2-5eb0`（guard-dangerous ブランチから分岐）。

### やったこと

- [x] C-1: `isSkill` を `.claude/skills/<name>/SKILL.md` に限定。補助 `.md`（reference.md 等）は frontmatter なしでも許可。
- [x] C-2: model に `inherit` / `opus` / `sonnet` / `haiku` / `fable` と既存の `claude-<family>-...` を許可。未知値は従来どおり拒否。
- [x] C-3: settings.json の `$CLAUDE_PROJECT_DIR/...` フック参照先の実在を検証。
- [x] `.claude/tests/validate-agent-config.test.mjs` を新設（5 件）。`node --test .claude/tests/*.test.mjs` は 96 件成功。PR #6。

### 次回やること

- [x] 残り任意: B-1 `record-subagent` に `agent_type`、D-1 所要時間の 48h 窓。→ セッション 7
- [ ] （持ち越し）ユーザーのアプリ要件を確認し、MVP と技術スタックを決める。

### 所要時間（セッション 6）

記録なし

---

## セッション 7（同日追記）: B-1 / D-1 / D-2 計測の穴

依頼: 「同じモデルで次にすすましょう」。モデルは Grok 4.6 のまま。
変更レベル: Level 1。ユーザー承認: 不要（既存フック・スクリプトの記録漏れ修正。継続依頼済み）。
ブランチ: `cursor/harness-instrumentation-b1-d1-5eb0`（validate-agent-config ブランチから分岐）。

### やったこと

- [x] B-1: `record-subagent.mjs` が SubagentStop の `agent_type`（無ければ `agent_name`）・`agent_id`・`agent_transcript_path` を jsonl に残す。`last_assistant_message` は本文になり得るので保存しない。
- [x] D-1: `estimate-session-time.mjs` の git 窓を「直近 48 時間」から、対象日の暦日（HARNESS_TZ）の `--since` / `--until` に変更。
- [x] D-2: `.claude/lib/harness-time.mjs` に `dayInTz` / `gitDayRange` を集約。`check-improvement-cycle` のクールダウンキー（旧 UTC `toISOString`）、`record-task-metrics.sh` の `date +%F`、`collect-task-metrics.mjs` のローカル `dayInTz` を同じ関数へ。
- [x] テスト 12 件を追加。`node --test .claude/tests/*.test.mjs` は 108 件成功。lint / type-check はアプリ未実装のため unknown。PR #7。

### 次回やること

- [ ] 残り任意: B-2 ConfigChange、B-3 SessionEnd/PreCompact、D-3 review-readiness テスト分割、H-1 quality-gates の pnpm フォールバック。
- [ ] （持ち越し）ユーザーのアプリ要件を確認し、MVP と技術スタックを決める。

### 所要時間（セッション 7）

記録なし
