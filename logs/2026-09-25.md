# 2026-09-25

## 今日のタスク

契約した Devin を Tomotabi の開発に組み込むため、Knowledge の文面と導入記録を用意する（L1。文書のみ。ユーザー承認: 不要）。

## やったこと

- [x] `docs/devin-setup.md` を新設した。役割分担（Devin は L0 / L1 と PR レビュー、Claude Code は L2 / L3）、Machine setup、Knowledge に登録する 6 項目の文面、未確認事項を書いた。
- [x] `AGENTS.md` の「ツールごとの入口」に Devin の項を追加した。
- [x] `docs/harness-setup.md` に Devin 追加の記録と参照先を書き足した。

## 追加作業: Devin 用スキル（PR #17 のマージ後）

Devin の Knowledge が Skills に移行していたため、画面への登録をやめ、リポジトリのスキルで届ける形に変えた（L1。ユーザー承認: 不要。ハーネス構成の変更として PR で重点レビュー）。

- [x] `.agents/skills/devin-workflow/SKILL.md` を追加した（Devin 専用。`.claude/skills/` には置かない）。
- [x] `docs/devin-setup.md` の Knowledge 節を「ルールを届ける方法」に置き換え、未確認事項を更新した（`AGENTS.md` の自動読み込みは確認済みとして削除、pstack の `.mdc` 取り込みの注意を追加）。
- [x] `AGENTS.md` の Devin の項と `docs/harness-setup.md` の記録を、スキルを指すように直した。

## 追加作業: Devin のスキル一覧の確認結果を反映（PR #18 のマージ後）

- [x] Devin がシンボリックリンク経由の共有スキルと `devin-workflow` を読めることを、`docs/devin-setup.md` に確認済みとして記録した。
- [x] Devin に Cookpit のスキルも見えているため、Tomotabi では使わないことを `devin-workflow` に追記した。

## 追加作業: Devin の作業環境（blueprint）（PR #19 のマージ後）

- [x] Devin の作業環境の設定が画面方式から `.devin/blueprint.yaml`（Git 管理）に変わっていたため、Tomotabi 用の blueprint を追加した（Node 22、`npm ci`、品質コマンドの参照）。
- [x] `docs/devin-setup.md` の Machine setup 節を blueprint の説明に置き換えた。

## 追加作業: 環境ビルドと main の保護の確認結果を反映（PR #20 のマージ後）

- [x] Devin の blueprint のビルドが成功したこと（Node 22、`npm ci`、Health Check）を `docs/devin-setup.md` に確認済みとして記録した。
- [x] ユーザーが main に Ruleset（PR 必須・CI 必須・force push と削除の禁止、バイパス無し）を設定したことを記録した。

## 追加作業: Issue #22 npm audit の high 解消（Devin）

- [x] `@nestjs/platform-express` を 11.2.3 → 11.2.6 に更新し、`multer` が 2.2.0 → 2.4.0 に上がった（`npm update`、メジャー不変）。
- [x] `undici` は orval を `^8.36.0` → `^8.37.0` に更新し、root `package.json` の `overrides` で `@scalar/json-magic` 配下の `undici` を `^7.29.1` に固定した。`@scalar/json-magic` が `undici@7.24.4` を pin しているため、overrides でのみ解消できる構成。
- [x] `npm audit`: high 3 件 → 0 件（moderate 10 件は Issue の対象外）。

## 検証

- `npm run test:harness`: 119 件成功、0 件失敗。
- prettier: `docs/devin-setup.md` は整形済み。`AGENTS.md` の警告は変更前からあるもので、今回は範囲外として触っていない。
- アプリのコードは変えていないため、lint / type-check / test / build は実行していない。
- 追加作業（Issue #22）: `npm run lint` / `npm run type-check` / `npm test` / `npm run build` / `npm run api:check` / `npm run db:check` すべて成功。`npm run test:api-db` は CI の api-db ジョブに任せる（未確認）。

## 気づき・メモ

- Devin が `AGENTS.md` を自動で読むか、Devin の VM で Docker が動くかは未確認。
- main のブランチ保護を GitHub で設定しておくと、Devin の誤 push を防げる。

## AI ツール活用記録

Claude Code: Devin の機能整理と、Knowledge の文面・導入記録の作成。サブエージェントは使っていない。

Devin: Issue #22 の対応。`npm audit` の high 3 件（multer・@nestjs/platform-express・undici）をメジャー更新なしで解消し、orval の `undici` は overrides で 7.29.1 に固定。PR はブランチ `devin/npm-audit-high-22`。

## 次回やること

- [ ] Devin に小さなタスクを 1 つ任せ、ルール（ブランチ名・PR 説明・ログ記録）が守られるか確かめる。候補: `npm audit` の high 3 件の調査と、破壊的変更の無い範囲での更新。
- [ ] PR #13 のレビューとマージ、M1 の実装計画・試験計画（前回からの持ち越し）。

## 所要時間

記録なし
